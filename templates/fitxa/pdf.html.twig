{% block body %}
    {% import 'fitxa/fitxa_macros.html.twig' as macros %}
    {{ macros.pdfHeaderSection(fitxa, app.request.locale) }}

    {% if eremuak.helburuatext %}
        {{ macros.bilingualTextSection(labelak.helburualabeleu, labelak.helburualabeles, fitxa.helburuaeu, fitxa.helburuaes, app.request.locale) }}
    {% endif %}

 
    {% if (eremuak.norkeskatutext or eremuak.norkeskatutable) %}
        {{ macros.bilingualListSection(
            labelak.norkeskatulabeleu,
            labelak.norkeskatulabeles,
            fitxa.norkeu,
            fitxa.norkes,
            fitxa.norkeskatuak,
            'norkeu',
            'norkes',
            eremuak.norkeskatutext,
            eremuak.norkeskatutable,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.dokumentazioatext or eremuak.dokumentazioatable) %}
        {{ macros.bilingualListWithLinkSection(
            labelak.dokumentazioalabeleu,
            labelak.dokumentazioalabeles,
            fitxa.dokumentazioaeu,
            fitxa.dokumentazioaes,
            fitxa.dokumentazioak,
            'estekaeu',
            'estekaes',
            'deskribapenaeu',
            'deskribapenaes',
            eremuak.dokumentazioatext,
            eremuak.dokumentazioatable,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.doklaguntext or eremuak.doklaguntable) %}
        {{ macros.bilingualListWithLinkSection(
            labelak.doklagunlabeleu,
            labelak.doklagunlabeles,
            fitxa.doklaguneu,
            fitxa.doklagunes,
            fitxa.doklagunak,
            'estekaeu',
            'estekaes',
            'deskribapenaeu',
            'deskribapenaes',
            eremuak.doklaguntext,
            eremuak.doklaguntable,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.kostuatext or eremuak.kostuatable) %}
        {{ macros.bilingualKostuaSection(
            labelak.kostualabeleu,
            labelak.kostualabeles,
            labelak.doanlabeleu,
            labelak.doanlabeles,
            eremuak.kostuatext,
            eremuak.kostuatable,
            fitxa,
            kostuZerrenda,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.ebazpensinpli or eremuak.arduraaitorpena or eremuak.aurreikusi or eremuak.arrunta or eremuak.isiltasunadmin) %}
        {{ macros.bilingualEpeSection(labelak, fitxa, eremuak, app.request.locale) }}
    {% endif %}
 
    {% if (eremuak.kanalatext or eremuak.kanalatable) %}
        {{ macros.bilingualKanalSection(
            labelak.kanalalabeleu,
            labelak.kanalalabeles,
            fitxa.kanalaeu,
            fitxa.kanalaes,
            kanalmotak,
            fitxa.kanalak,
            fitxa.zerbitzua,
            fitxa.udala,
            fitxa.parametroa,
            eremuak.kanalatext,
            eremuak.kanalatable,
            app.request.locale
        ) }}
    {% endif %}
 
    {% if (eremuak.prozeduratext or eremuak.prozeduratable) %}
        {{ macros.bilingualProzeduraSection(
            labelak.prozeduralabeleu,
            labelak.prozeduralabeles,
            fitxa.prozeduraeu,
            fitxa.prozeduraes,
            fitxa.prozedurak,
            eremuak.prozeduratext,
            eremuak.prozeduratable,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.araudiatext or eremuak.araudiatable) %}
        {{ macros.bilingualAraudiaSection(
            labelak.araudialabeleu,
            labelak.araudialabeles,
            fitxa.araudiaeu,
            fitxa.araudiaes,
            fitxa.araudiak,
            eremuak.araudiatext,
            eremuak.araudiatable,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.datuenbabesatext or eremuak.datuenbabesatable) %}
        {{ macros.bilingualDatuenBabesSection(
            labelak.datuenbabesalabeleu,
            labelak.datuenbabesalabeles,
            fitxa.udala,
            fitxa.datuenbabesa,
            fitxa.datuenbabesaeu,
            fitxa.datuenbabesaes,
            eremuak.datuenbabesatext,
            eremuak.datuenbabesatable,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.besteak1text or eremuak.besteak1table) %}
        {{ macros.bilingualLinkListSection(
            labelak.besteak1labeleu,
            labelak.besteak1labeles,
            fitxa.besteak1eu,
            fitxa.besteak1es,
            fitxa.besteak1ak,
            'estekaeu',
            'estekaes',
            'izenburuaeu',
            'izenburuaes',
            eremuak.besteak1text,
            eremuak.besteak1table,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.besteak2text or eremuak.besteak2table) %}
        {{ macros.bilingualLinkListSection(
            labelak.besteak2labeleu,
            labelak.besteak2labeles,
            fitxa.besteak2eu,
            fitxa.besteak2es,
            fitxa.besteak2ak,
            'estekaeu',
            'estekaes',
            'izenburuaeu',
            'izenburuaes',
            eremuak.besteak2text,
            eremuak.besteak2table,
            app.request.locale
        ) }}
    {% endif %}

    {% if (eremuak.besteak3text or eremuak.besteak3table) %}
        {{ macros.bilingualLinkListSection(
            labelak.besteak3labeleu,
            labelak.besteak3labeles,
            fitxa.besteak3eu,
            fitxa.besteak3es,
            fitxa.besteak3ak,
            'estekaeu',
            'estekaes',
            'izenburuaeu',
            'izenburuaes',
            eremuak.besteak3text,
            eremuak.besteak3table,
            app.request.locale
        ) }}
    {% endif %}

{% endblock %}