{% extends 'base.html.twig' %}

{% block menuTitle %}
    <a class="navbar-brand">{{ 'messages.espedientekudeaketa'| trans }}</a>
{% endblock menuTitle %}

{% block menuOptions %}
    <ul class="nav navbar-nav">
        <li><a href="#" onclick="document.forms['{{ edit_form.vars.name }}'].submit()" class="fa fa-floppy-o"> {{ 'messages.gorde'| trans }}</a></li>
        <li><a href="{{ path('espedientekudeaketa_index') }}" class="fa fa-undo"> {{ 'messages.zerrendara'| trans }}</a></li>
    </ul>
{% endblock menuOptions %}

{% block body %}
    <div class="container" role="main">
            
        {{ form_start(edit_form) }}
        <div class="row">
            <div class="col-sm-6">
                <div class="form-group">
                    <b>{{ 'messages.izena'| trans }} ({{ 'messages.eu'| trans }})</b>
                    {#{{ form_label(edit_form.izenaeu, 'Izena (eu): ', { 'label_attr': {'class': 'control-label'} }) }}#}
                    {{ form_errors(edit_form.izenaeu) }}
                    {{ form_widget(edit_form.izenaeu, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>
            <div class="col-sm-6">
                <div class="form-group">
                    <b>{{ 'messages.izena'| trans }} ({{ 'messages.es'| trans }})</b>
                    {#{{ form_label(edit_form.izenaes, 'Izena (es): ', { 'label_attr': {'class': 'control-label'} }) }}#}
                    {{ form_errors(edit_form.izenaes) }}
                    {{ form_widget(edit_form.izenaes, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-sm-6">
                <div class="form-group">
                    <b>{{ 'messages.esteka'| trans }} ({{ 'messages.eu'| trans }})</b>
                    {#{{ form_label(edit_form.urleu, 'URL (eu): ', { 'label_attr': {'class': 'control-label'} }) }}#}
                    {{ form_errors(edit_form.urleu) }}
                    {{ form_widget(edit_form.urleu, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>
            <div class="col-sm-6">
                <div class="form-group">
                    <b>{{ 'messages.esteka'| trans }} ({{ 'messages.es'| trans }})</b>
                    {#{{ form_label(edit_form.urles, 'URL (es): ', { 'label_attr': {'class': 'control-label'} }) }}#}
                    {{ form_errors(edit_form.urles) }}
                    {{ form_widget(edit_form.urles, {'attr': {'class': 'form-control'}}) }}
                </div>
            </div>
        </div>
        {{ form_end(edit_form) }}
    </div>
{% endblock body %}
